#include <stdio.h> 
//header file library 
//stdio = standard input/output -> let us work with input and output function, such as printf()

//C ignore blank space, we use it to make our code more readable
int main() { //main() is a function, everything inside its curly bracket {} will be executed
    printf("Hello World!\n"); //printf() is an output function -> thanks to stdio.h we are able to use it
    return 0; //return 0 ends the main function
}